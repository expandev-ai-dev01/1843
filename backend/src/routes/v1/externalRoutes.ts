import { Router } from 'express';

/**
 * @summary
 * External (public) API routes configuration.
 * Handles authentication and public endpoints.
 *
 * @module routes/v1/externalRoutes
 */

const router = Router();

/**
 * @remarks
 * Public routes will be registered here.
 * Examples:
 * - POST /api/v1/external/auth/register
 * - POST /api/v1/external/auth/login
 * - POST /api/v1/external/auth/recover-password
 */

export default router;
